# khmer_pdf_shaper_example

Demonstration Flutter application showing how to render complex Khmer Unicode text in PDF documents using `khmer_pdf_shaper` and `printing`.

## Running the Example

```bash
cd example
flutter run
```
